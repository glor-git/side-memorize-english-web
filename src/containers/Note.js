import React from 'react';
import styled from "styled-components";

function Note(props) {
  return (
    <Button>ㅋㅋㅋ</Button>
  );
}

const Button = styled.button`
  background: red;
`;


export default Note;